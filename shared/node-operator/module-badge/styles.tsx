import styled from 'styled-components';
import { BadgeStyle } from '../role-badge/styles';

export const BadgeModuleStyle = styled(BadgeStyle)`
  background: var(--lido-color-accentControlBg);
  color: var(--lido-color-text);
  font-weight: 600;
  padding-inline: 6px;
`;
