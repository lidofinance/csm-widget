import type { ComponentType } from 'react';

// Jest stand-in for svgr imports (see moduleNameMapper in jest.config.cjs)
export const ReactComponent: ComponentType = () => null;

export default ReactComponent;
