name: Forked Test CM-widget
run-name: Forked CM-widget Tests. Env [${{ github.event.inputs.stand_type || 'testnet' }}]

on:
  # TEMP (remove before merge): allow running this workflow from the feature branch
  push:
    branches:
      - tests/add-cm-schedule
  schedule:
    - cron: "0 6 * * *" # Every day at 6 AM UTC
  workflow_dispatch:
    inputs:
      stand_type:
        description: "Stand type"
        required: true
        type: choice
        default: testnet
        options:
          - testnet
          - preview
      preview_stand_url:
        description: 'Preview stand url (only if "Stand type" is "preview")'
        required: false
        type: string
      preview_stand_env:
        description: 'Preview stand env (only if "Stand type" is "preview")'
        required: false
        type: choice
        default: testnet
        options:
          - testnet
          - infra

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      # Required envs
      RPC_URL: ${{ secrets.RPC_URL }}
      WALLET_SECRET_PHRASE: ${{ secrets.WALLET_SECRET_PHRASE }}
      WALLET_PASSWORD: ${{ secrets.WALLET_PASSWORD }}
      EMPTY_SECRET_PHRASE: ${{ secrets.EMPTY_SECRET_PHRASE }}
      STAND_TYPE: ${{ inputs.stand_type || github.event.inputs.STAND_TYPE || 'testnet' }}
      WC_PROJECT_ID: ${{ secrets.WC_PROJECT_ID }}
      MODULE: cm

      # Common envs
      QASE_API_TOKEN: ${{ secrets.QASE_API_TOKEN }}
      QASE_PROJECT_ID: "CM"
      GH_ACTION_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      GH_BRANCH_REF_NAME: ${{ github.ref_name }}
      GH_EVENT_NAME: ${{ github.event_name }}
      NODE_OPTIONS: --max-old-space-size=4096
      TEST_TAGS: "forked"
      TEST_BRANCH: ${{ github.event.inputs.branch }}
      REPORT_ENABLED: "${{ github.event_name == 'schedule' && 'true' || 'false' }}"
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      SLACK_DUTY_TAG: "S09H269H2TT"
      ARTIFACT_PASSWORD: ${{ secrets.ARTIFACT_PASSWORD }}

      # Optional envs
      REFUSE_CF_BLOCK_NAME: ${{ secrets.REFUSE_CF_BLOCK_NAME }}
      REFUSE_CF_BLOCK_VALUE: ${{ secrets.REFUSE_CF_BLOCK_VALUE }}
      PUSHGATEWAY_URL: ${{ secrets.PUSHGATEWAY_URL }}
      PUSHGATEWAY_USERNAME: ${{ secrets.PUSHGATEWAY_USERNAME }}
      PUSHGATEWAY_PASSWORD: ${{ secrets.PUSHGATEWAY_PASSWORD }}
      GRAFANA_URL: ${{ secrets.GRAFANA_URL }}
      GRAFANA_API_KEY: ${{ secrets.GRAFANA_API_KEY }}

      # Forked test envs
      USE_FORK: true
      DEPLOY_CONFIG: ./artifacts/hoodi/curated/deploy-hoodi.json
      CHAIN: hoodi
      ARTIFACTS_DIR: ./artifacts/hoodi/
      PINATA_API_KEY: ${{ secrets.QA_PINATA_API_KEY }}
      PINATA_API_SECRET: ${{ secrets.QA_PINATA_API_SECRET }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "yarn"

      - name: Checkout community-staking-module
        uses: actions/checkout@v6
        with:
          repository: lidofinance/community-staking-module
          path: community-staking-module
          ref: test/local-scripts

      - name: Setup just
        uses: ./.github/actions/setup-just

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.7.1

      - name: Restore yarn cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node_modules-${{ hashFiles('**/yarn.lock') }}

      - name: Install CM Contracts dependencies
        run: |
          cd community-staking-module && yarn install --immutable

      - name: Build CM Fork
        run: cd community-staking-module && just deps && just build

      - name: Run e2e tests
        uses: ./.github/actions/run-e2e-tests
        with:
          module: cm
          preview-stand-url: ${{ inputs.preview_stand_url || github.event.inputs.preview_stand_url }}
          preview-stand-env: ${{ github.event.inputs.preview_stand_env }}
          preview-stand-login: ${{ secrets.PREVIEW_STAND_LOGIN }}
          preview-stand-password: ${{ secrets.PREVIEW_STAND_PASSWORD }}
